/*
#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    double num1{} ;
    
    cout << "enter the num here in (double) : " ; 
    cin >> num1 ;
   
    cout << "the square root of the num is : " << sqrt(num1) << endl;
    cout << "the cube root of the num is : " << cbrt(num1) << endl;
   
    cout << "the sin of the num is " << sin(num1) << endl;
    cout << "the cos of the num is " << cos(num1) << endl;

    cout << "the ceil of the num is " << ceil(num1) << endl;
    cout << "the floor of the num is " << floor(num1) << endl;
    cout << "the round of the num is " << round(num1) << endl;
    
    double power{} ;
    cout << "enter the power to raise " << num1 << "to : " << endl;
    cin >> power ;
    cout << num1 << " raise to the " << power << " power to " << pow(num1,power) << endl;
    
    
    
    cout << endl;
    return 0;
}


// cout << "-----------------------------------" << endl;


#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std;

int main()
{
  int random_number{} ;
  size_t count{10} ;
  int min{1} ;
  int max{5} ; 
  
  cout << "random_max on my system is : " << RAND_MAX << endl;
  srand(time(nullptr)) ;
  
  for(size_t i{1} ; i <= count ; ++i)
  {
   random_number = rand()%max + min ;
   cout << random_number << endl;
   }
   cout << endl;
   return 0;
}



cout << "--------------------------------" << endl;


#include <iostream>
#include <cmath>

using namespace std;
const double pi{3.14159} ;

double calc_area_circle(double radius) 
{
  return pi*pow(radius,2.00);
}

void area_circle()
{
 double radius{};
 cout << "enter the radii here : " ;
 cin >> radius ;
 cout << "the area of a circle with radius " << radius << " is " << calc_area_circle(radius)  << endl; 
}

double calc_volume_cylinder(double radius ,double height)
{
    return pi*pow(radius,2.00)*height ; 
}

void volume_cylinder()
{
 double radius{};
 double height{};

 cout << "enter the height of the cylinder " ; cin >> height ;
 cout << "enter the radii of the cylinder " ; cin >> radius ;
 
 cout << "the volume of the cylinder is " << calc_volume_cylinder(height,radius) << endl;
 
} 

int main()
{
    area_circle();
    volume_cylinder();
    
    return 0;
}

*/


c
